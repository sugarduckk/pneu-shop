import styled from 'styled-components';

const DropIconContainer = styled.div`
  width: 1.4em;
  height: 1.4em;
  position: absolute;
  right: 1em;
  top: 50%;
  transform: translateY(-50%);

`;

export default DropIconContainer;