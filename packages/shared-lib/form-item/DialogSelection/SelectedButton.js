import styled from 'styled-components';
import SimpleCard from '../../layout/SimpleCard';

const SelectedButton = styled(SimpleCard)`
  position: relative;
  cursor: pointer;
`;

export default SelectedButton;