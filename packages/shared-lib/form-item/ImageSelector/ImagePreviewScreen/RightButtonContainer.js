import styled from 'styled-components';
import IndexContainer from './DecoratorContainer';

const RightButtonContainer = styled(IndexContainer)`
  right: 0;
  bottom: 0;
  border: none;
`;

export default RightButtonContainer;