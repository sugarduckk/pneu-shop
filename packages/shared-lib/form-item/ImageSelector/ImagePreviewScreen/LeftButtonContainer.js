import styled from 'styled-components';
import DecoratorContainer from './DecoratorContainer';

const LeftButtonContainer = styled(DecoratorContainer)`
  left: 0;
  bottom: 0;
  border: none;
`;

export default LeftButtonContainer;